{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "Jobs": {
    "OpportunityRecompute": {
      "//": "Players is deprecated — watchlist now lives in tracked_players (see docs/watchlist.md). Populate via data/players-catalog.seed.json or POST /api/watchlist.",
      "Players": []
    }
  }
}
